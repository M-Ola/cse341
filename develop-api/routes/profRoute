
const express = require("express");
const router = express.Router();
const { getProfessionalData } = require("../controllers/profController");

router.get("/", getProfessionalData);

module.exports = router;
